<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="interdroid.swancore">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <receiver
            android:name="interdroid.swancore.engine.EvaluationEngineReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="interdroid.swan.REGISTER" />
                <action android:name="interdroid.swan.UNREGISTER" />
                <action android:name="interdroid.swan.NOTIFY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <service
            android:name=".engine.EvaluationEngineServiceBase"
            android:exported="false" />

    </application>

</manifest>
